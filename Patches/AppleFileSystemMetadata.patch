--- a/llvm/lib/Support/Unix/Path.inc
+++ b/llvm/lib/Support/Unix/Path.inc
@@ -112,6 +112,10 @@
 #define STATVFS_F_FLAG(vfs) (vfs).f_flag
 #else
 #define STATVFS_F_FLAG(vfs) (vfs).f_flags
+#endif
+
+#if defined(SWIFT_TOOLCHAIN_APPLE_FILE_METADATA)
+#include "AppleFileSystemMetadata.h"
 #endif
 
 using namespace llvm;
@@ -476,6 +480,7 @@
   return std::error_code();
 }
 
+#if !defined(SWIFT_TOOLCHAIN_APPLE_FILE_METADATA)
 static bool is_local_impl(struct STATVFS &Vfs) {
 #if defined(__linux__) || defined(__GNU__) || defined(__managarm__)
 #ifndef NFS_SUPER_MAGIC
@@ -560,22 +565,34 @@
 #endif
 }
 
+#endif
+
 std::error_code is_local(const Twine &Path, bool &Result) {
+#if defined(SWIFT_TOOLCHAIN_APPLE_FILE_METADATA)
+  return std::error_code(swift_toolchain_is_local_path(Path.str().c_str(), &Result),
+                         std::generic_category());
+#else
   struct STATVFS Vfs;
   if (::STATVFS(const_cast<char *>(Path.str().c_str()), &Vfs))
     return errnoAsErrorCode();
 
   Result = is_local_impl(Vfs);
   return std::error_code();
+#endif
 }
 
 std::error_code is_local(int FD, bool &Result) {
+#if defined(SWIFT_TOOLCHAIN_APPLE_FILE_METADATA)
+  return std::error_code(swift_toolchain_is_local_fd(FD, &Result),
+                         std::generic_category());
+#else
   struct STATVFS Vfs;
   if (::FSTATVFS(FD, &Vfs))
     return errnoAsErrorCode();
 
   Result = is_local_impl(Vfs);
   return std::error_code();
+#endif
 }
 
 std::error_code rename(const Twine &from, const Twine &to) {
